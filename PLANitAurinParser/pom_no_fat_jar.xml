<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	
	<parent>
    <groupId>org.planit.aurin.parserwrapper</groupId>
    <artifactId>PLANitAurinParserWrapper</artifactId>
		<version>0.2.1a1</version>
	</parent>
	
	<groupId>org.planit.aurin.parser</groupId>
	<artifactId>PLANitAurinParser</artifactId>
	<description>Expose functionality used by Aurin ATRC platform regarding the OSM to MATSim converter</description>
	<packaging>jar</packaging>

	<name>PLANitAurinParser</name>
	<url>https://github.com/AURIN-OFFICE/PLANitOsmParserWrapper</url>
	
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  
    <matsim.version>12.0</matsim.version>
    <planit.version>${project.version}</planit.version>
    <junit.version>4.13-beta-2</junit.version>
    
    <!-- Maven plugin properties -->
    <maven.compiler.java.version>11</maven.compiler.java.version>    
    
    <!-- Maven plugin versions -->
    <maven.compiler.plugin.version>3.8.1</maven.compiler.plugin.version>
    <maven.jar.plugin.version>3.2.0</maven.jar.plugin.version>
    <maven.resources.plugin.version>3.1.0</maven.resources.plugin.version>
  </properties> 	
	
	<!-- license based on the one published on the website -->
	<licenses>
		<license>
			<name>PLANit license based on modified BSD</name>
			<url>${planit.website.url}/docs/licenses/LICENSE.txt</url>
			<distribution>repo</distribution>
			<comments>A slightly modified version of a modified BSD license</comments>
		</license>
	</licenses>

	<!-- versions inherited from parent -->
	<dependencies>

		<dependency>
			<groupId>org.planit</groupId>
			<artifactId>PLANit</artifactId>
			<version>${planit.version}</version>
		</dependency>

		<dependency>
			<groupId>org.planit.osm</groupId>
			<artifactId>PLANitOSM</artifactId>
			<version>${planit.version}</version>
		</dependency>

		<dependency>
			<groupId>org.planit.matsim</groupId>
			<artifactId>PLANitMatsim</artifactId>
			<version>${planit.version}</version>
		</dependency>
		
    <dependency>
        <groupId>org.matsim</groupId>
        <artifactId>matsim</artifactId>
        <version>${matsim.version}</version>
    </dependency> 		
		
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <optional>true</optional>
      <version>${junit.version}</version>
    </dependency> 		

	</dependencies>
	
  <repositories>
    <repository>
      <!-- Repository for MATSim releases (MATSim is not on Maven central) -->
      <id>matsim</id>
      <url>https://repo.matsim.org/repository/matsim</url>
    </repository>
  </repositories>   	

  <build>

    <resources>
      <resource>
        <directory>src/main/resources</directory>
      </resource>
    </resources>

    <plugins>
      <plugin>
        <artifactId>maven-resources-plugin</artifactId>
        <version>${maven.resources.plugin.version}</version>
        <executions>
          <!-- inherit from parent plugin management configuration -->
          <execution>
            <id>copy-resources</id>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>${maven.jar.plugin.version}</version>
        <configuration>
          <archive>
            <manifest>
              <!-- no need for class path since dependencies are all included in shaded jar -->
              <addClasspath>false</addClasspath>
              <mainClass>org.planit.aurin.parser.PlanitAurinParserMain</mainClass>
            </manifest>
            <manifestEntries>
            </manifestEntries>
          </archive>
        </configuration>
      </plugin>
      
       <plugin>
         <groupId>org.apache.maven.plugins</groupId>
         <artifactId>maven-compiler-plugin</artifactId>
         <version>${maven.compiler.plugin.version}</version>
         <configuration>
           <source>${maven.compiler.java.version}</source>
           <target>${maven.compiler.java.version}</target>
           <release>${maven.compiler.java.version}</release>
         </configuration>
       </plugin>       

    </plugins>
  </build>
</project>